# ============================================
# FoodValue AI - Environment Configuration
# ============================================
# SECURITY WARNING: Never commit this file with actual values!
# Always use secure methods to set production environment variables.

# Supabase Configuration
# These are automatically available in Supabase Edge Functions
VITE_USE_SUPABASE=true
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Sheets Integration (Optional)
# Only required if using Google Sheets sync functionality
VITE_GOOGLE_SHEETS_API_KEY=your_google_sheets_api_key
VITE_GOOGLE_SHEET_ID=your_google_sheet_id

# OpenAI API Configuration
# REQUIRED: For AI Chat functionality and report generation
# For frontend (used in development)
VITE_OPENAI_API_KEY=your_openai_api_key_here
# For Supabase Edge Functions (server-side)
OPENAI_API_KEY=your_openai_api_key_here

# SMTP Configuration (REQUIRED for email functionality)
# CRITICAL: These MUST be set in Supabase Dashboard as secrets
# NEVER hardcode credentials in code
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_FROM_EMAIL=your_from_email
SMTP_FROM_NAME=FoodValue AI

# CORS Configuration (Production)
# CRITICAL: Set to your production domain in production environment
# Development: Use '*' or 'http://localhost:5173'
# Production: Use 'https://yourdomain.com'
ALLOWED_ORIGIN=*

# Development
NODE_ENV=development